export const userInfo = state => state.user;

export const currentSong = state => state.currentSong;

export const playList = state => state.playList;
export const seqList = state => state.sequenceList;
export const currentIndex = state => state.currentIndex;
export const mode = state => state.playMode;

export const currentList = state => state.currentList;
export const historyKeyWords = state => state.hisKeyList;
export const currentSinger = state => state.currentSinger;
export const getMore = state => state.getMore;
export const playerShow = state => state.playerShow;
export const fmMode = state => state.fmMode;
